//! Выгрузка в `ex_system` (этап 06 — `promts/06_sync_agent.md`).
//!
//! Возобновляемая чанковая выгрузка записи в Django (`Settings.sync.*`):
//! размер чанка (`chunk_size_mb`), оффлайн-очередь (`offline_queue.enabled`),
//! ретраи с экспоненциальным бэкоффом (`retry.*`) и статус-поллинг. Контракт
//! API определяет этап `07_backend_integration.md` (в репозитории `ex_system`).
//!
//! На этапе 00 — заготовка; сетевого взаимодействия нет.
