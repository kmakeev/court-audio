//! Встроенный проигрыватель сессий (этап 10.1 — `promts/10_1_playback.md`).
//!
//! Ядро воспроизведения: чистая модель таймлайна сессии/дорожки
//! ([`timeline`]), потоковый декодер сегментов с транспортом на `rodio`
//! ([`source`]), конечный автомат плеера ([`state`]) и аудит доступа
//! ([`audit`]). Слой Tauri (`crate::ipc::player_cmds`) — тонкая обвязка
//! поверх этого модуля, как и для `recorder`/`store`/`integrity`.
//!
//! Приоритет захвата: плеер использует выходное аудио-устройство и
//! собственный `PlayerState`-мьютекс, независимый от `CaptureState`/
//! `MonitorState` (входное устройство записи) — параллельная запись и
//! прослушивание не делят ни поток, ни устройство.

pub mod audit;
pub mod source;
pub mod state;
pub mod timeline;
