# Статус проекта «Аудиопротокол»

Снимок состояния на **2026-07-05**. Обновлять при смене этапа/крупной вехи.

## Сводка

- **v1 (этапы 00–08)** — завершён; релиз `v0.1.0` (draft).
- **Улучшения после v1 (этапы 09, 10, 10.1–10.6)** — реализованы; версия
  поднята до **`v0.2.0`** (многоканал, живая разметка, плеер, экспорт,
  авторизация, роли настроек, адаптивный UI, UX-пакет).
- **Бэклог проверки на станции (этапы 13.1–13.7)** — реализован и выпущен
  релизом **`v0.3.0`** (2026-07-05): правки UI, превью многоканала, структура
  зеркала, зависание HTML-плеера, fail-secure гейтов + обязательный ключ станции,
  автономный оффлайн-старт по PIN (B-001) + Windows overlay (R-006), шифрование
  сегментов в live-пути (R-013) + потоковый FLAC-экспорт (R-014). Источник —
  [`backlog.md`](backlog.md); журнал — [`../CHANGELOG.md`](../CHANGELOG.md).
- **Повторная проверка на Windows-станции (2026-07-05).** По R-006 найден корень
  зависания overlay — создание окна из **синхронной** Tauri-команды дедлочит цикл
  событий на Windows/Linux; overlay-команды сделаны `async` (приёмка — на
  Windows-станции). По B-001 закрыт тупик первого запуска изолированного зала:
  флаг автономного режима недостижим через UI (экран «Администрирование» за
  гейтом входа) — теперь провижинится env `COURT_AUDIO_AUTONOMOUS_OFFLINE=1` или
  готовым [`samples/settings.autonomous_offline.json`](samples/settings.autonomous_offline.json).
- Первый запуск в полностью оффлайн-режиме задокументирован —
  [`first_run_offline.md`](first_run_offline.md).
- Анализ ресурсоёмкости и требования к железу (2026-07-05) —
  [`hardware.md`](hardware.md); релизный профиль сборки оптимизирован
  (LTO, −21 % размера бинаря). Находки анализа — R-013 (шифрование сегментов не
  подключено в live-путь) и R-014 (FLAC-экспорт держит сессию в памяти) —
  **реализованы** этапом 13.7 ([`../promts/13_7_flac_memory_and_segment_encryption.md`](../promts/13_7_flac_memory_and_segment_encryption.md)),
  в составе `v0.3.0`.

## v1 (этапы 00–08) — завершён

Ядро захвата, надёжность/бесперебойность, хранилище + целостность, UI,
привязка к делу, выгрузка в `ex_system`, упаковка и дистрибутивы — реализованы.
Журнал изменений — [`../CHANGELOG.md`](../CHANGELOG.md).

## CI — зелёный

[`../.github/workflows/ci.yml`](../.github/workflows/ci.yml): матрица
**macOS + Ubuntu + Windows**, шаги `fmt` → `clippy` → `check` → фронт →
`tauri build` → артефакты прогона. Ветка сборки — `main`.

По ходу первого запуска устранены 4 инфраструктурных дефекта CI (не логика
приложения):
1. формат `case_cmds.rs` под rustfmt;
2. `shell: bash` для Windows `--config` (дефолт раннера — pwsh);
3. `libasound2-dev` — ALSA-заголовки для `cpal` на Linux;
4. `"path": null` в Windows-fallback (`--config` мержится по RFC 7386, иначе
   остаётся `path` от `fixedRuntime` и ломает схему).

## Релизы — draft по тегу

[`../.github/workflows/release.yml`](../.github/workflows/release.yml): пуш тега
`vX.Y.Z` собирает матрицу и публикует **черновик** GitHub Release с
установщиками (публикацию подтверждает человек). Детали и оговорки —
[`packaging.md`](packaging.md) → «Релизы».

**Выпущен `v0.1.0` (черновик).** Ассеты (5): `.deb`, `.rpm`, `.AppImage`,
`.exe` (NSIS), `.dmg` (aarch64, dev). Черновик до публикации имеет временный
URL `releases/tag/untagged-…` и не виден на странице тега `v0.1.0`; после
**Publish** привяжется к тегу.

**`v0.3.0` — тег запушен (2026-07-05).** Матрица release.yml собирает черновик с
установщиками; сборку и публикацию оператор контролирует вручную. Содержимое —
этапы 13.1–13.7 + повторная проверка на станции (R-006 async, B-001 bootstrap).
Качество ассетов — прежнее (неподписанные, Windows онлайн-вариант), пока не
заданы секреты (см. «Открытые пункты»).

> Артефакты сейчас **тестовые**: неподписанные; Windows — онлайн-вариант
> (`downloadBootstrapper`), т.к. секреты не заданы. Пользователь скачивает
> черновик для изучения; вопрос боевого качества — далее.

## Открытые пункты до боевой поставки

- **Секреты** (Repository secrets) и подключение к release-джобе:
  `WEBVIEW2_FIXED_URL` (оффлайн-Windows), Windows code signing, `GPG_*` (Linux).
- **Отечественные ОС**: сборка `.deb`/`.rpm` на стенде Astra SE / РЕД ОС
  ([`package-domestic.yml`](../.github/workflows/package-domestic.yml)).
- **Опционально**: `msi`-таргет; ASCII `mainBinaryName` (сейчас имена ассетов
  на GitHub теряют кириллицу — косметика).
- **Финальные** bundle identifier / правообладатель (плейсхолдер
  `ru.court.audioprotocol`) — от заказчика; чек-лист
  [`registry-checklist.md`](registry-checklist.md).
- **Руководство оператора/администратора** — черновики готовы
  ([`operator_guide.md`](operator_guide.md), [`admin_guide.md`](admin_guide.md));
  перед подачей в Реестр — вычитка и скриншоты с целевой станции.

## Фаза 2 — частично реализована

Из фазы-2 этапов реализованы `09_multichannel` (многоканал по ролям) и
`10_markers_realtime` (живая разметка); `11_gost_signing` и `12_video` остаются
перспективой (промты готовы в [`../promts/`](../promts/)). Дорожная карта —
[`../CLAUDE.md`](../CLAUDE.md).

## Улучшения после v1 — реализованы (v0.2.0)

Рекомендации по развитию ([`recommendations.md`](recommendations.md)) закрыты
этапами `10_1_playback` … `10_6_ux_polish`: проигрыватель с метками, экспорт
папка/DVD, авторизация через `ex_system`, разнесение настроек админ/оператор,
адаптивность, UX-пакет. Серверный `07` дозавершён в `ex_system` (2026-07-05):
мультитрек (09), разметка (10) и признак `autonomous_offline` (13.6, решение
«а» — `operator_id` = PK реального пользователя) приняты; сверка контракта
клиент↔сервер расхождений не находит.
